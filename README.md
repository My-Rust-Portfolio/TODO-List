# Todo List
A program allowing users to manage their tasks. Supports addition, completion, deleteion and listing of the tasks.  

## Usage
cargo run -- add "item" "description" (eg. add "milk" "buy")  
cargo run -- list  
cargo run -- complete #id (eg. complete 1)  
cargo run -- delete #id (eg. delete 1)  
cargo test  